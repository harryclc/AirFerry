{
  "name": "@airferry/web",
  "version": "1.2.2",
  "description": "AirFerry web sender — static website that streams QR codes (reuses sender source).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "extract-lzma-wasm": "node scripts/extract-lzma-wasm.cjs",
    "postinstall": "node scripts/extract-lzma-wasm.cjs",
    "prepare-wasm": "node scripts/prepare-wasm.cjs",
    "predev": "node scripts/prepare-wasm.cjs",
    "prebuild": "node scripts/prepare-wasm.cjs",
    "prebuild:receiver": "node scripts/prepare-wasm.cjs",
    "prebuild:standalone": "node scripts/prepare-wasm.cjs",
    "dev": "vite",
    "build": "vite build",
    "build:receiver": "vite build --config vite.receiver.config.ts",
    "build:standalone": "vite build --config vite.standalone.config.ts && node scripts/build-standalone.cjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@foxglove/wasm-zstd": "^1.0.1",
    "lzma-wasm": "^1.0.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zxing-wasm": "^3.1.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
